- name: First Tests
  hosts: localhost
  gather_facts: yes
  vars_files:
    - secrets.yml
  roles:
    - test_ghost
  tags:
    - ghost

- name: Second Tests
  hosts: localhost
  gather_facts: yes
  vars_files:
    - secrets.yml
  roles:
    - test_post
  tags:
    - post

- name: Third Tests
  hosts: localhost
  gather_facts: yes
  vars_files:
    - secrets.yml
  roles:
    - test_forget
  tags:
    - forget